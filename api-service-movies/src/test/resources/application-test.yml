server:
  port: 8081

spring:
  config:
    activate:
      on-profile: test
  application:
    name: local-movie-service
  data:
    mongodb:
      uri: mongodb+srv://jlagoria:Shenkun1981@cluster0.vvfax.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: movie_test
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    org.springframework: INFO